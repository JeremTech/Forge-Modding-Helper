<UserControl x:Class="FMH.Core.View.AssistantCreator.AssistantCreatorWorkspaceTechnicalSettingsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FMH.Core.View.AssistantCreator"
             xmlns:controls="clr-namespace:FMH.Core.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="433"
             d:DesignWidth="650">
    
    <Grid x:Name="MainGrid"
          Margin="10">

        <Grid.RowDefinitions>

            <RowDefinition Height="50" />
            <RowDefinition Height="55" />
            <RowDefinition Height="55" />
            <RowDefinition Height="55" />
            <RowDefinition Height="55" />
            <RowDefinition Height="120" />

        </Grid.RowDefinitions>

        <!-- Header text -->
        <StackPanel Grid.Row="0" 
             Orientation="Vertical">

            <TextBlock Tag="assistant_creator.technical_settings.label.description1"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource FontColorPrimary}"
                       TextWrapping="WrapWithOverflow"
                       Text="It's time for technical settings of your workspace !"/>

            <TextBlock Tag="assistant_creator.technical_settings.label.description2"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource FontColorPrimary}"
                       TextWrapping="WrapWithOverflow"
                       Text="Be carefull, some of theses settings will couldn't be changed after the generation !"/>

        </StackPanel>

        <!-- Mod id -->
        <StackPanel Grid.Row="1"
                    Orientation="Vertical">

            <TextBlock Tag="assistant_creator.technical_settings.label.mod_id"
                       HorizontalAlignment="Left"
                       Foreground="{DynamicResource FontColorPrimary}"
                       Text="ModId"
                       Margin="0, 0, 0, 5"/>
            <controls:InputTextBox x:Name="ModIdTextBox"
                                   Grid.Row="1" 
                                   Height="25" 
                                   PlaceHolderText="mymod"
                                   PlaceHolderTextTranslationKey="assistant_creator.technical_settings.label.mod_id.placeholder"
                                   InputText="{Binding NewWorkspaceData.ModId, Mode=TwoWay}"/>

        </StackPanel>

        <!-- Mod group -->
        <StackPanel Grid.Row="2"
                    Orientation="Vertical">

            <TextBlock Tag="assistant_creator.technical_settings.label.mod_group"
                       HorizontalAlignment="Left"
                       Foreground="{DynamicResource FontColorPrimary}"
                       Text="ModGroup"
                       Margin="0, 0, 0, 5"/>
            <controls:InputTextBox x:Name="ModGroupTextBox"
                                   Grid.Row="1" 
                                   Height="25" 
                                   PlaceHolderText="net.me.mymod"
                                   PlaceHolderTextTranslationKey="assistant_creator.technical_settings.label.mod_group.placeholder"
                                   InputText="{Binding NewWorkspaceData.ModGroup, Mode=TwoWay}"/>

        </StackPanel>

        <!-- Mod version -->
        <StackPanel Grid.Row="3"
                    Orientation="Vertical">

            <TextBlock Tag="assistant_creator.technical_settings.label.mod_version"
                       HorizontalAlignment="Left"
                       Foreground="{DynamicResource FontColorPrimary}"
                       Text="Mod version"
                       Margin="0, 0, 0, 5"/>
            <controls:InputTextBox x:Name="ModVersionTextBox"
                                   Grid.Row="1" 
                                   Height="25" 
                                   PlaceHolderText="1.0.0.0"
                                   PlaceHolderTextTranslationKey="assistant_creator.technical_settings.label.mod_version.placeholder"
                                   InputText="{Binding NewWorkspaceData.ModVersion, Mode=TwoWay}"/>

        </StackPanel>

        <!-- Mod update JSON link -->
        <StackPanel Grid.Row="4"
            Orientation="Vertical">

            <StackPanel Orientation="Horizontal"
                        Margin="0, 0, 0, 5">
                <TextBlock Tag="assistant_creator.technical_settings.label.mod_json_update_link"
                           HorizontalAlignment="Left"
                           Foreground="{DynamicResource FontColorPrimary}"
                           Text="Update JSON file link"/>
                <TextBlock Tag="assistant_creator.label.optional"
                           HorizontalAlignment="Left"
                           Foreground="{DynamicResource FontColorSecondary}"
                           FontStyle="Italic" 
                           Text="(optional)"
                           Margin="3, 0, 0, 0"/>
            </StackPanel>

            <controls:InputTextBox x:Name="ModUpdateJsonFileTextBox"
                                   Grid.Row="1" 
                                   Height="25" 
                                   PlaceHolderText="https://website.net/updates.json"
                                   PlaceHolderTextTranslationKey="assistant_creator.technical_settings.label.mod_json_update_link.placeholder"
                                   InputText="{Binding NewWorkspaceData.UpdateJsonFileLink, Mode=TwoWay}"/>

        </StackPanel>

    </Grid>
    
</UserControl>
