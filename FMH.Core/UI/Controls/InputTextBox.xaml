<UserControl x:Class="FMH.Core.UI.Controls.InputTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:FMH.Core.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="25" 
             d:DesignWidth="300"
             Background="{DynamicResource InputsBackgroundColor}">

    <Border BorderBrush="{Binding StatusColorBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
            BorderThickness="{Binding StatusBorderThickness, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="25"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBox x:Name="TextBoxControl"
                     BorderThickness="0"
                 Grid.Column="0"
                 Panel.ZIndex="2"
                 Foreground="{DynamicResource FontColorPrimary}"
                 CaretBrush="{DynamicResource FontColorPrimary}"
                 Background="Transparent"
                 VerticalAlignment="Stretch"
                 VerticalContentAlignment="{Binding InputTextVerticalAlignement, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 Text="{Binding InputText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 AcceptsReturn="{Binding IsMultiline, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 VerticalScrollBarVisibility="Auto"
                 Padding="2, 0" />

            <TextBlock Grid.Column="0"
                   Text="{Binding PlaceHolderText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Visibility="{Binding PlaceHolderVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   VerticalAlignment="{Binding InputTextVerticalAlignement, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Margin="5, 1"
                   Foreground="{DynamicResource FontColorSecondary}"
                   Background="Transparent"
                   Opacity="0.5"
                   FontStyle="Italic" />

            <fa:ImageAwesome Grid.Column="1"
                             Height="15"
                             Icon="{Binding StatusIcon, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             Foreground="{Binding StatusColorBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
                             Visibility="{Binding StatusIconVisibility, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

        </Grid>
    </Border>

</UserControl>
