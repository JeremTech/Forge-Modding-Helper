<UserControl x:Class="FMH.Core.UI.Controls.StandardButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FMH.Core.UI.Controls"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="50"
             d:DesignWidth="100"
             Padding="5">

    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="StandardButton" TargetType="{x:Type Border}">
                <Setter Property="Background" Value="{DynamicResource StandardButtonColor}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource StandardButtonHoveredColor}"/>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.5"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <UserControl.InputBindings>
        <MouseBinding MouseAction="LeftClick" Command="{Binding Command, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding CommandParameter, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
    </UserControl.InputBindings>

    <Border CornerRadius="5" Style="{DynamicResource StandardButton}">
        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">

            <fa:FontAwesome FontSize="10" Icon="{Binding LeftIcon, RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="{DynamicResource FontColorPrimary}" VerticalAlignment="Center"/>
            <Label FontSize="12" Content="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="{DynamicResource FontColorPrimary}" VerticalAlignment="Center"/>
            <fa:FontAwesome FontSize="10" Icon="{Binding RightIcon, RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="{DynamicResource FontColorPrimary}" VerticalAlignment="Center"/>

        </StackPanel>
    </Border>

</UserControl>
