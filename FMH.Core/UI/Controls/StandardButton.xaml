<UserControl x:Class="FMH.Core.UI.Controls.StandardButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FMH.Core.UI.Controls"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="50"
             d:DesignWidth="100"
             Padding="5">

    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="StandardButton" TargetType="{x:Type Border}">
                <Setter Property="Background" Value="#003A55"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#006494"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <UserControl.InputBindings>
        <MouseBinding MouseAction="LeftClick" Command="{Binding Command}" CommandParameter="{Binding CommandParameter}"/>
    </UserControl.InputBindings>

    <Border CornerRadius="5" Style="{DynamicResource StandardButton}">
        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">

            <fa:FontAwesome FontSize="10" Icon="{Binding LeftIcon}" Foreground="{DynamicResource FontColorPrimary}" VerticalAlignment="Center"/>
            <Label FontSize="12" Content="{Binding Text}" Foreground="{DynamicResource FontColorPrimary}" VerticalAlignment="Center"/>
            <fa:FontAwesome FontSize="10" Icon="{Binding RightIcon}" Foreground="{DynamicResource FontColorPrimary}" VerticalAlignment="Center"/>

        </StackPanel>
    </Border>

</UserControl>
