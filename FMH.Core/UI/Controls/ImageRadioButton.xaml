<RadioButton x:Class="FMH.Core.UI.Controls.ImageRadioButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FMH.Core.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="100" 
             d:DesignWidth="100">

        <RadioButton.Style>
            <Style TargetType="RadioButton">
                <Style.Triggers>
                    <Trigger Property="IsChecked" Value="False">
                        <Setter Property="Opacity" Value="0.5"></Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </RadioButton.Style>

        <RadioButton.Template>
            <ControlTemplate TargetType="RadioButton">

                <Border CornerRadius="8" BorderThickness="1" Background="{DynamicResource InputsBackgroundColor}">
                
                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>

                        <Image Source="{Binding Image, RelativeSource={RelativeSource AncestorType=RadioButton}}"
                               Grid.Row="0"
                               Width="60"
                               Height="60"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Text, RelativeSource={RelativeSource AncestorType=RadioButton}}"
                                   FontSize="12"
                                   Grid.Row="1"
                                   Foreground="{DynamicResource FontColorPrimary}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </ControlTemplate>
        </RadioButton.Template>

</RadioButton>
